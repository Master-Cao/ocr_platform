# 模板匹配配置 (templatematch)
# 修改后无需重新编译，程序运行时会自动读取

# 最大匹配数量
max_count=1

# 得分阈值 [0,1]，低于此值的匹配将被过滤
score_threshold=0.5

# 重叠框 IOU 去重阈值
iou_threshold=0.0

# 匹配角度范围（度），360 表示全方向旋转
angle=360

# 顶层金字塔最小面积
min_area=256

# 顶层角度步长（度），顶层粗搜使用，精搜阶段仍用小步长
# 设为 0 时自动根据顶层模板尺寸计算（推荐）；>0 时使用指定值
top_angle_step=0
